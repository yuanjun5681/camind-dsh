export const name = 'ui-foundation'

// Host half intentionally empty. Declaring a bundle row makes the browser
// client module available to the graph; all behavior lives in lib/client.js.
export function apply() {}
