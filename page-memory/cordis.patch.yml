# Bundle layer: insert the memory page plugin row.
# Plugin rows reference the package by name so Node resolution finds the installed code.
- insert:
    - id: page-memory
      name: camind-page-memory
