# Bundle layer: insert the machine registry plugin row.
# Plugin rows reference the package by name so Node resolution finds the installed code.
- insert:
    - id: machine
      name: camind-service-machine
