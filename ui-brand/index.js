// camind-ui-brand Host half — the brand mark is a pure browser contribution
// (ui-sidebar's sidebar.brand seat), so Host activation is intentionally empty.
export const name = 'ui-brand'

export function apply() {}
